<!DOCTYPE html>
<html lang="eng">
  <head>
    <meta charset="UTF-8">
    <title>Users Index</title>
  </head>
  <body>
    <h1>Handlebars</h1>
    <ul>
      {{#each users as |user|}}
      <li>
        <a href="/user/{{user.username}}">{{user.name.full}}</a>
      </li>
      {{/each}}
    </ul>
  </body>
</html>